# Claude Desktop config snippet for multistate-mcp-server
#
# Drop into:
#   macOS: ~/Library/Application Support/Claude/claude_desktop_config.json
#   Windows: %APPDATA%/Claude/claude_desktop_config.json
#
# Required env (placeholders only — load real values from OS keychain):
#   MULTISTATE_MCP_BEARER_JWT
#   MULTISTATE_MCP_ORDERS_SVC_URL
#   LANGSMITH_API_KEY
